# AI-Douyin-Agent 依赖清单
# tkinter 为 Python 自带，无需安装

Pillow>=10.0      # 图片处理：封面渲染 + 代码/生活照标签 + Logo 绘制
openpyxl>=3.1     # 生成/读写 07_发布记录.xlsx

# BGM 节奏感知截取（分析音频节拍→自动截取连贯片段）
pydub>=0.25       # 音频加载/裁剪/淡入淡出
numpy>=1.24       # 能量/节拍分析
imageio-ffmpeg>=0.4  # 自带 ffmpeg 二进制，免去系统级安装

# 自动发布到抖音（Playwright 浏览器自动化 + AI 视觉识别）
playwright>=1.40  # 浏览器控制：打开抖音/上传图片/填写内容
requests>=2.25    # HTTP 调用 DeepSeek Vision API（AI 看截图找按钮）
urllib3>=1.26     # SSL 兼容（Python 3.10 需要）
